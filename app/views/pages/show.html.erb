<h1><%= @page.name %></h1>

<%= @page.content.html_safe %>

<% if admin_signed_in? %>
	<div data-no-turbolink>
		<%= link_to 'Редактировать', edit_page_path(@page), class: "admin-edit-link-right" %>
	</div>
<% end %>
